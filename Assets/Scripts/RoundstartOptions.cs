using UnityEngine;

// Just some idea that popped in my head that probably can't be implemented until way later but...
// Each character would have unique roundstart actions they can take to execute within the first
// few frames of a round. These would be tied to an Rock-paper-scissors interaction where the player
// can choose between engaging in low risk or high risk options.

public class RoundstartOptions : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start() { }

    // Update is called once per frame
    void Update() { }
}
